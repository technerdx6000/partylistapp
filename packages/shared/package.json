{
    "name": "@listcollab/shared",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "lint": "eslint .",
        "test": "vitest run --root ../.. --config vitest.config.ts --project shared",
        "test:coverage": "vitest run --root ../.. --config vitest.config.ts --project shared --coverage",
        "test:watch": "vitest --root ../.. --config vitest.config.ts --project shared",
        "type-check": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "zod": "3.25.76"
    },
    "devDependencies": {
        "typescript": "5.9.2"
    }
}